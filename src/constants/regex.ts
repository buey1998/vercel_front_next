/**
 * @description Uppercase Lowercase Spacebar EN only
 */
export const commonPattern = /[^A-Za-z0-9\s]/gi

/**
 * @description wallet address
 */
export const addressPattern = /^0x[a-fA-F0-9]{40}$/
